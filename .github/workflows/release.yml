name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Package version label (without v)"
        required: true
        default: "0.0.0"

permissions:
  contents: write

jobs:
  package-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Resolve version
        id: ver
        env:
          EVENT_NAME: ${{ github.event_name }}
          REQUESTED_VERSION: ${{ inputs.version }}
        run: |
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            version="$REQUESTED_VERSION"
          else
            version="${GITHUB_REF_NAME#v}"
          fi
          PACKAGE_VERSION="$version" node scripts/package.cjs --help >/dev/null
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: QA
        run: node scripts/qa.cjs
        env:
          PACKAGE_VERSION: ${{ steps.ver.outputs.version }}

      - name: Package
        run: node scripts/package.cjs --output-root dist
        env:
          PACKAGE_VERSION: ${{ steps.ver.outputs.version }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.ver.outputs.version }}
          name: design-pipeline v${{ steps.ver.outputs.version }}
          generate_release_notes: true
          files: |
            dist/design-pipeline-skill.tgz
            dist/design-pipeline-skill.zip
            dist/checksums.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
