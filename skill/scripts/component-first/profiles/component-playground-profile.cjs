"use strict";

const COMPONENT_PLAYGROUND_PROFILE = Object.freeze({
  id: "component-first-playground",
  version: 1,
  kind: "component",
  stage: "integration",
  required: true,
});

module.exports = { COMPONENT_PLAYGROUND_PROFILE };
