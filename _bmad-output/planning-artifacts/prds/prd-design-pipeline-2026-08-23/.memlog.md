---
topic: Design Pipeline multi-source design tool and resource support
updated: 2026-08-24T00:28
---

- (decision) PRD scope is the design pipeline supporting multiple design tools and resource sites through local artifacts, not an MCP service.
- (assumption) Fast path is used because the user supplied a concrete architecture boundary and current implementation context; unresolved choices are tagged in the PRD.
- (decision) MVP includes DesignMD five-kind synchronization and governed local import paths for Figma, Penpot, and other design tools; upstream execution and MCP are non-goals.
- (decision) Terminology decision: use design tool importer or Tool Connector for pipeline-local integration; do not describe it as an MCP adapter or service.
- (event) Memlog audit complete: all four entries are represented in PRD or addendum; phase blockers remain explicit.
- (decision) MVP 不自动 hydrate 上游 Skill 源码；DesignMD 只内化页面/元数据和来源，源码安装必须走单独显式、许可证绑定的流程。
- (decision) Figma/Penpot v1 只接受用户本地导出 JSON/SVG/PNG/tokens，不接远端 API、不登录、不写回云端文件。
- (decision) 首个外部设计工具连接器优先 Figma 本地导出；网页提取能力沿用现有管线，Penpot 复用同一导入契约后接入。
- (event) PRD update/finalize pass started on 2026-08-24. Existing PRD draft is the source of record; downstream SPEC, Architecture, and epics are consumers, not new product inputs.
- (event) Memlog audit: all prior decisions and assumptions are represented in prd.md or addendum.md; no unlogged decision was found. Input reconciliation: no additional user-supplied product document was introduced in this finalize pass.
- (event) Open-item triage: Figma/Penpot exact field mappings, source modes, and fidelity thresholds remain downstream implementation questions already captured in SPEC/Architecture; they do not block PRD scope or non-goals.
- (change) PRD review fixes: canonicalize Resource Source, Evidence Receipt, contentSha256/sourceSha256, SHA-256 wording, and lowercase admission statuses.
- (change) PRD review fixes: split Design Artifact Normalization from Pipeline Route and Handoff; assign stable NFR-1 through NFR-13 and align the epics inventory.
- (decision) PRD v1 Design Tool support matrix is local-file only for Figma and Penpot with JSON/SVG/PNG/tokens; remote API, login, writeback, plugin export, and archive modes are not v1 ready.
- (change) Final prose gate fix: canonical sync and review status tokens are used in PRD, addendum, and epics; prose synonyms are no longer contract states.
- (event) PRD finalized after structure PASS and local prose/status gate; downstream SPEC, architecture, epics, and implementation contract are reconciled for v1 local-file scope.
