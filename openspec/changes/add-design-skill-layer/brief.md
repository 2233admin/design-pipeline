# Brief: Manifest-Driven Design Skill Layer

Add single-purpose design skills whose machine-readable manifests enforce effects and whose typed
handoff artifacts are evaluated by deterministic gates rather than trusted as pass declarations.
