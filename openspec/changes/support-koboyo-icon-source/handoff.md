# Handoff

Current phase: release readiness.

The source registry, resolver, CLI exposure, OpenSpec evidence, full QA, packaging, and isolated
installation checks are complete. Koboyo remains an explicit-only reviewed route; no credentials,
icon corpus, MCP runtime, or remote mutations are authorized by this change. Replacing the globally
installed skill remains a separate operator action.
