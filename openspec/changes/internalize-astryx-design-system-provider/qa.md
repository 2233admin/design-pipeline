# QA

## Status

Complete. The implementation, compatibility, security, benchmark, packaging, isolated-install,
installed-CLI, and real Astryx acquisition checks passed. The change is ready for user acceptance
testing.

## Focused Contract Checks

- [x] Snapshot/catalog normalization is deterministic and non-mutating.
- [x] Unknown versions/fields, duplicate IDs, path escape, absolute paths, executable values,
  prototype-pollution keys, and hash tampering fail closed.
- [x] Search filters are deterministic and do not mutate the catalog.
- [x] Token projection preserves representable modes/roles and reports every review/blocking loss.
- [x] Runtime decisions enforce existing-project precedence, compatibility, intake, canary, strict
  catalog validation, and blocked fallback rules.

Evidence:

- Focused design-system, benchmark, provider, and CLI suite: 63/63 passed.
- Full manifest-driven Node suite after review fixes: 248/248 passed.
- Bundled Astryx snapshot regenerated byte-identically from official CLI JSON.
- Snapshot SHA-256:
  `4b44b96f42d022fa1cad6cbfd255178d4138bef31701659192ba54af95ab28cb`.
- Bundled counts: 154 components, 42 hooks, 20 docs, 655 templates, and 184 projected tokens with
  zero loss and `ready` status.

Commands:

```powershell
node --test tests/astryx-bundled-snapshot.test.cjs tests/design-system-catalog.test.cjs tests/design-system-decision.test.cjs tests/design-system-provider.test.cjs tests/benchmark.test.cjs tests/designer-pipeline-cli.test.cjs
$manifest = Get-Content scripts/test-manifest.json -Raw | ConvertFrom-Json
$testFiles = @($manifest.tests | ForEach-Object { Join-Path tests $_ })
node --test $testFiles
node scripts/generate-astryx-snapshot.cjs $env:TEMP\astryx-snapshot-cli-921d3db $env:TEMP\astryx-snapshot-regenerated.json
```

## Local Acquisition Checks

- [x] Default Astryx acquisition invokes only the bundled read-only translator over an explicit
  contained provider CLI path; custom acquisition invokes only an explicit contained `.js`/`.mjs`
  adapter.
- [x] Only read-only manifest/component/docs/template/hook requests are allowed.
- [x] No shell is used; timeout, output size, environment filtering, provider/API/license identity,
  entry status, and output containment are enforced.
- [x] A missing/escaping `--provider-cli-path` fails before the bundled translator runs.
- [x] Install/init/swizzle/upgrade/codemod/theme-build, `AGENTS.md` injection, and direct provider
  documentation module execution are rejected.
- [x] Failed acquisition records failure and publishes no partial successful snapshot or catalog.

Real `@astryxdesign/cli@0.2.0` evidence:

- Status `complete`; 154 components, 42 hooks, 21 docs including the provider manifest, 655
  templates, 872 normalized catalog entries, and zero failures.
- Provider CLI SHA-256:
  `6efd6c9a8b09fa5acd0c639fc27120f6ac64bbd23eac3d55515912f97b3b568b`.
- Live snapshot SHA-256:
  `9f3b99ecd82517ed4ec4a87f23a8184da5c6f3a2695d84de823921278f57d5a3`.
- `provider.json` SHA-256:
  `159870a626aaef32f53f018d6d7ec51336382f4245f4de23cee1225003c4f977`.

Command:

```powershell
node skill/scripts/designer-pipeline.cjs design-system acquire --root $env:TEMP\astryx-snapshot-cli-921d3db --provider-cli-path clients/cli/bin/astryx.mjs --output-root provider-smoke --timeout-ms 60000 --json
```

## Public CLI Checks

- [x] Help lists `design-system profiles|normalize|acquire|search|project-tokens|decide`.
- [x] Frozen flags and `design-pipeline.cli-result.v1` envelopes match the user guide.
- [x] Normalize, projection, and decision are non-writing without `--write`.
- [x] Acquisition writes only below explicit `--output-root`.
- [x] Installed-package smoke covers the bundled catalog, supplied snapshot normalization, local
  adapter acquisition, projection, decision, benchmark brief, help, and doctor surfaces.

An isolated installed copy normalized all 871 bundled entries from a supplied snapshot and acquired
a local adapter into a five-entry catalog with a complete receipt. The hermetic archive install and
installed CLI suite also passed under invalid proxies and an isolated home.

The global Codex Skill installation was then atomically replaced from the verified workspace source
at `C:\Users\Administrator\.codex\skills\design-pipeline`. Its 134-file tree is byte-identical to
`skill/`; installed doctor, dependency check, Astryx profile discovery, `AlertDialog` search, and
184-token projection all passed. The prior global copy remains recoverable at
`C:\Users\ADMINI~1\AppData\Local\Temp\design-pipeline-global-backup-20260802-010759`.

## Benchmark v2 Checks

- [x] v1 manifest/measurement behavior and result shape remain compatible.
- [x] v2 requires all six fairness conditions.
- [x] Required failures and missing evidence override aggregate scores.
- [x] Invalid fairness and stable/prerelease mixing cannot pass without explicit
  `allowCanaryMix: true`.
- [x] `systemChannels` must exactly cover systems with known channels, and result evidence retains
  fairness/channel validation and invalid reasons.
- [x] Developer briefs and results do not expose private expectations.

## Repository And Release Checks

- [x] Focused Node tests.
- [x] Full `node scripts/qa.cjs`.
- [x] Schema and package manifest parity.
- [x] Reproducible tgz/zip/checksum output.
- [x] Isolated package installation with invalid proxies and no ambient credentials.
- [x] Installed `designer-pipeline` public CLI smoke.
- [x] Repository status unchanged by read-only QA paths.
- [x] Strict OpenSpec validation.

Final release evidence:

- `node scripts/qa.cjs`: exit 0.
- Repository tests inside hermetic QA: 248/248 passed.
- Reproducible artifacts: tgz 602,458 bytes; zip 4,371,254 bytes; matching checksums across two
  independent package roots.
- Isolated install, explicit replacement, dependency self-check, and installed CLI test: passed.
- `openspec validate internalize-astryx-design-system-provider --strict`: passed.
- Public state CLI: phase `release-readiness`, status `complete`, revision 10, history `consistent`;
  state SHA-256 `7c8da0e163c1e5707136e2d43ee7fea7298b0a9b7067865e4d3e22a4f1bcee93`.
- Design foundation SHA-256:
  `1126499915fb720ce0943afcba8b9327885c11a6ccc63160db96cd801a8cf88e`.
- Motion foundation SHA-256:
  `5a23e0fcdb7d4ebddea2f8f446b91edbdaba3812baf44a7066034b5b2d8e7302`.
- Code Intel artifacts:
  `C:\Users\Administrator\AppData\Local\code-intel\artifacts\design-pipeline\1785598854065-101076-core\run-complete.json`
  and
  `C:\Users\Administrator\AppData\Local\code-intel\artifacts\astryx-code-intel-019fbdf60e23\1785599084022-102028-core\run-complete.json`.

## Residual Risk Review

- [x] Documentation states that the adapter boundary is not an OS network sandbox.
- [x] Astryx profile metadata, pinned revision
  `921d3db31ae4323e0625d2f5a95c32fe07a3d5d7`, version 0.2.0, and MIT attribution are recorded.
- [x] No provider artifact is described as overriding `DESIGN.md` or `MOTION.md`.
- [x] Independent review was available and all actionable findings were resolved.

Independent code review initially found two high and one medium issue: benchmark channel mixing,
provider entry-status admission, and duplicate weak catalog validation. All three were fixed and
regression-tested. Final review verdict: **APPROVE**, with zero critical/high/medium findings.

Remaining operational boundaries:

- A custom local adapter is executable code; use an offline or OS-sandboxed host when network
  isolation must be proven.
- The bundled snapshot is pinned evidence, not an updater. Refresh requires an explicit official CLI
  acquisition, regeneration, license/status review, and new verification.
- The global Skill now contains the verified implementation. Codex discovers newly installed Skill
  content on the next turn; restart the app only if the host keeps an older session-level cache.
- No Issue, pull request, release, or other remote publication was created.
