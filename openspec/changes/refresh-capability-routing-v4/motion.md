# Motion

No product UI or runtime motion is implemented by this repository change.

The change updates the motion decision contract only:

- Anime.js v4.5 capability routing;
- runtime selection against CSS, GSAP, React View Transitions, and existing libraries;
- cleanup, reduced-motion, accessible text, render-loop, and deterministic evidence requirements.
