# Directions

Alternative directions are intentionally waived. The user selected the primary live page as the exact implementation authority, so the captured source direction is inherited without stylistic reinterpretation.
