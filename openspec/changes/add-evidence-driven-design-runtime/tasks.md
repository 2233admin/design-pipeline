# Tasks

- [ ] Add the declarative control phase registry and preserve legacy phase aliases.
- [ ] Implement deterministic design-plan compilation and input validation.
- [ ] Implement artifact metadata validation, hashing, and contained-path checks.
- [ ] Implement idempotent downstream invalidation and stale-cause reporting.
- [ ] Extend the existing state extension boundary with phase-status and outcome semantics.
- [ ] Add mandatory default-plus-non-default interaction-state coverage validation.
- [ ] Route plan, run, resume, verify, status, explain-block, and package through the existing CLI envelope.
- [ ] Add focused contract tests for determinism, blocking, stale propagation, state coverage, and packaging.
- [ ] Run the declared repository QA command and inspect the final diff for placeholders or scope escapes.
