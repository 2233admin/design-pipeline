# Tasks

- [x] Add failing tests: a referenced change whose `design.md` has no reconciliation section reports gate-review blocked.
- [x] Reorder the `primary-target` flow in SKILL.md so the graybox precedes change `design.md`.
- [x] Require `design.md` to cite the graybox capture it was written against.
- [x] Add the required `Spec Reconciliation` section to `design-spec.md`.
- [x] Require a `Cause` column entry describing an observation, not an intention.
- [x] Accept an empty reconciliation table; reject an absent section.
- [x] Add the Spec reconciliation row to `qa-checklist.md`.
- [ ] Verify the reconciliation gate against the archived `jst-hud-clock` run as a regression fixture.
- [x] Run focused tests, full tests, and package QA.
