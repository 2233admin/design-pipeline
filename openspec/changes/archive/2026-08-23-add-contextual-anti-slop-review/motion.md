# Motion

This change adds no user-interface motion and no animation runtime.

The rubric covers content visibility without animation and reduced-motion verification as hard
quality rules. The evaluator itself is a deterministic CLI and has no motion behavior.
