# Directions: Decouple Component-First Gate

Visual direction preview is waived because the change is non-visual. The committed engineering
direction is an effect shell around a pure, deterministic conformance core with versioned output.
