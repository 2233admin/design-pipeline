# Proposal

Extend the existing frontend-stack tool registry and resolver. Do not add a second icon registry or
an MCP client: the governed route is enough to make the source discoverable while preserving the
existing authority boundary for external credentials and remote mutations.
