# Handoff

Status: implementation complete; final repository, package, installed-skill, and independent-review
verification pending.

Resume by running:

1. `node --test tests/*.test.cjs`
2. `node scripts/qa.cjs`
3. packaged and installed `check-deps.cjs --json`

If any foundation contract changes, update the canonical spec, change delta, schemas, tests, and
resource registry together.
