# Direction

Visual direction is inherited and waived as non-visual. The committed product direction is a
deterministic, inspectable CLI contract: explicit selections, closed compatibility errors, pinned
source evidence, and no ambient installation side effects.
