# Handoff

Current phase: release readiness.

The mandatory public chain is `options -> resolve-stack -> decompose -> route -> decide`. The
repository and packaged skill pass full QA. External candidates remain inert until their recorded
runtime and authority requirements are satisfied. Replacing the globally installed skill is a
separate operator action; it was not performed by this change.
